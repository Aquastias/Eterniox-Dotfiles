#!/usr/bin/env bash

# shellcheck source=/dev/null

# Add additional directories to the PATH
export PATH="$HOME/bin:/usr/local/bin:$PATH"

# Add Python user bin directory to the PATH
export PATH="$HOME/.local/bin:$PATH"